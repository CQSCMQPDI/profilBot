## profilBot
Un simple bot avec gestion de profils pour discord

#### Configuration Mysql:

* 1 table
    * Columns:
        * pseudo (varchar)
        * picture (text)
        * presentation (text)
        * langage (varchar)
        * website (varchar)
        * banner (text)